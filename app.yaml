# app.yaml
name: ai-medical-content-generator
region: nyc

services:
  - name: web
    github:
      repo: zainal89/ai-medical-content-generator
      branch: main
    run_command: python main.py
    environment_slug: python
    instance_size_slug: basic-xs
    envs:
      - key: OPENAI_API_KEY
        scope: RUN_TIME
        value: ${OPENAI_API_KEY}
      - key: PERPLEXITY_API_KEY
        scope: RUN_TIME
        value: ${PERPLEXITY_API_KEY}
      - key: FIRECRAWL_API_KEY
        scope: RUN_TIME
        value: ${FIRECRAWL_API_KEY}
      - key: MONGODB_URI
        scope: RUN_TIME
        value: ${MONGODB_URI}

workers:
  - name: scheduler
    github:
      repo: zainal89/ai-medical-content-generator
      branch: main
    run_command: python worker.py
    environment_slug: python
    instance_size_slug: basic-xs
    instance_count: 1
    envs:
      - key: OPENAI_API_KEY
        scope: RUN_TIME
        value: ${OPENAI_API_KEY}
      - key: FIRECRAWL_API_KEY
        scope: RUN_TIME
        value: ${FIRECRAWL_API_KEY}
      - key: MONGODB_URI
        scope: RUN_TIME
        value: ${MONGODB_URI}
