ai-medical-content-generator/


├── app.yaml              # App Platform configuration


├── requirements.txt      # Dependencies for both Service and Worker


├── main.py              # FastAPI app (Service)


├── worker.py            # Scheduler script (Worker)


├── tasks.py             # Shared logic for fetch_and_store_topics


├── .env                 # Local environment variables (not committed)


└── Procfile             # For Heroku-style process definitions
